Kwerenda 1: SELECT nazwa, miasto FROM lokale;
Kwerenda 2: SELECT id, nazwa, cena FROM dania WHERE typ=3;
Kwerenda 3: SELECT MIN(cena) FROM dania WHERE typ=2;
Kwerenda 4: UPDATE lokale SET numer=23 WHERE id=1;